server {
    listen 80;
    
    location / {
        root /usr/share/nginx/html;
        index index.html index.htm;
        # This tells Nginx: If you can't find a file, just serve index.html and let React Router handle it!
        try_files $uri $uri/ /index.html;
    }
}